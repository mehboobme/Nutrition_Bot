from setuptools import setup, find_packages

setup(
    name="advance-rag-project",
    version="0.1.0",
    packages=[
        "ui",
        "core",
        "data",
        "agents",
        "services",
        "workflows"
    ],
    install_requires=[
        # your dependencies here
    ],
)
